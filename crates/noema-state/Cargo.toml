[package]
name = "noema-state"
description = "Versioned world state and generation transactions for Noema"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
noema-ir = { path = "../noema-ir" }
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
